import assert from "assert"

describe('ClassName', function() {
  describe('#methodName()', function() {
    it('should write assumption for code into here', function() {
      assert.equal("hello", `hello`);
    });
  });
});